### Add Google Analytics gtag.js tracking

Insert the provided Google Analytics 4 gtag.js snippet (including both the external script tag and the inline config script) into the `<head>` section of `index.html`, placed immediately before the closing `</head>` tag.

### Technical details
- **File:** `index.html`
- **Placement:** Inside `<head>`, before `</head>`
- **Content:** The exact GA4 tracking code provided by the user (`G-F66S83KN8G`)